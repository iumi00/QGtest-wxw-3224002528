说明：放置好层级模型之后点击上传项目传数据！！！

不足：
删除层级功能未实现，删除后后面的层级数字要改变，可能可以通过获取层级的数组，获取每个层级在数组中的序号来改变
功能除了修改模型权重基本都实现了，权重输入之后数值类型判断以及即时的修改有问题，还没仔细看
上传项目后删除功能还没做，项目应该和层级模型什么的绑定没做..
布局有些地方不是很满意，还需要调一下（有些感觉还不会），比较严重的问题是层级之间，同一行的层级的高度会随着最长的那一个层级一起变化，不是很好看
上传图片后缩略图的放置也不是很好...